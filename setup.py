from setuptools import setup

setup(
    name='hunsum-eval',
    version='1.0',
    # packages=['hunsum_eval', 'hunsum_eval.utils', 'hunsum_eval.embeddings', 'hunsum_eval.entrypoints',
    #          'hunsum_eval.metrics'],
    url='',
    license='',
    author='Dorina Lakatos',
    author_email='',
    description='',
    install_requires=[
        'summ-eval'
    ]
)
